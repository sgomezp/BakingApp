<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_margin="@dimen/activity_margin"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_image_recipe"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:contentDescription="@string/recipes_s_image"
        android:src="@drawable/ic_placeholder_cake_black" />

    <TextView
        android:id="@+id/tv_name_recipe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:paddingEnd="10dp"
        android:paddingStart="10dp"
        android:layout_toEndOf="@id/iv_image_recipe"
        tools:text="Recipe's name" />

    <TextView
        android:id="@+id/tv_servings_recipe"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:layout_toEndOf="@id/iv_image_recipe"
        android:layout_below="@id/tv_name_recipe"
        tools:text = "Servings"/>



</RelativeLayout>